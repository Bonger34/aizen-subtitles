# 《罗布奥特曼》·爱染诚 复刻运行手册

> 本仓库由 [Cicada000/VV](https://github.com/Cicada000/VV) 复刻而来，复用其全部代码与架构，
> 仅做最小改动：把视频源换成《罗布奥特曼》、识别人物换成**爱染诚**。
> 目标形态：**本地可运行版**（不部署 Vercel / Cloudflare，语义检索与 RAG 为可选）。

---

## 0. 整体原理（一句话）

```
视频(罗布奥特曼) ──main.py──► 每秒抽帧 + 爱染诚人脸相似度(编码进文件名) + PaddleOCR字幕
        │
        ▼
subtitle/[P###]标题.json   （条目: timestamp / similarity / text）
        │
        ├─ compress_subtitle.py ─► subtitle_db.gz  ──► 前端本地库(IndexedDB)
        │
        └─ merge_screenshot + 二分索引 + HTTP Range ─► 单帧 webp 拼图预览
        │
        ▼
Web 前端（搜索 / 复制 / 下载表情包）
```

---

## 1. 环境准备

### 1.1 Python 依赖（离线构建管线 + 本地 Flask API）
```bash
cd VV_Rob
pip install -r requirements.txt
# 额外需要：
pip install insightface onnxruntime paddleocr opencv-python pillow numpy tqdm
# 若用本地语义检索(FAISS)：
pip install torch transformers sentence-transformers faiss-cpu
```

### 1.2 Rust（可选，本地 /search 高速检索）
```bash
cd VV_Rob/api/search
cargo build --release
# 产物复制到 api/ 目录，命名为 subtitle_search_api
cp target/release/subtitle_search_api ../api/subtitle_search_api
```

### 1.3 前端静态服务（任意一种）
- Python：`python -m http.server 8080`（在 `VV_Rob/Web` 目录启动）
- 或 Node：`npx serve Web`

---

## 2. 第一步：获取视频（BBDown，B 站）

> 合规提示：仅限个人学习用途，注意版权。

1. 安装 [BBDown](https://github.com/nilaoda/BBDown)，登录 B 站账号。
2. 找到《罗布奥特曼》番剧，下载 **1080p（1920×1080）** 剧集。
   - ⚠️ **分辨率硬约束**：`CutSubtitle*.py` 内 `REQUIRED_RESOLUTION = (1920, 1080)`，
     非 1080p 的帧会被直接跳过、字幕为空。务必选 1080p。
3. 把视频文件放入 `VV_Rob/Videos/`，文件名建议带集数，例如：
   ```
   Videos/罗布奥特曼_01.mp4
   Videos/罗布奥特曼_02.mp4
   ```
   （`main.py` 会以"文件名去扩展名"作为字幕 JSON 的标题前缀。）

### 探针建议（最低成本、最高信息量）
先下 **1 集** 跑通，确认两件事再批量：
- 分辨率是否 1920×1080；
- 字幕条的实际像素坐标（见第 4 步），决定是否改 `params.py` 的 `SUBTITLE_AREA`。

---

## 3. 第二步：构建爱染诚人脸特征（核心）

原 `face_features_insightface.npz` 是**张维为**的特征，必须替换。

### 3.1 抽帧生成人脸候选
```bash
cd VV_Rob
python tools/extract_faces.py --interval 5 --max-faces 1
```
- 从 `Videos/` 每 5 秒抽 1 帧，检测并裁剪人脸到 `faces_candidates/`。
- 该脚本**只检测不比对**，所以 `target/` 为空也能跑。

### 3.2 人工挑选
打开 `faces_candidates/`，挑出**爱染诚的清晰正脸**（避开皮套/侧脸/遮挡），
复制到 `target/` 目录：
```bash
cp faces_candidates/罗布奥特曼_01_f000300_r0.jpg target/
# ……挑选 20~50 张质量稳定的正脸
```

### 3.3 生成特征向量（覆盖原文件）
```bash
python generate_features_insightface.py
# 生成 face_features_insightface.npz（爱染诚）
```

> 调参：`generate_features_insightface.py` 的 `det_thresh=0.5`；若漏检可下调。

---

## 4. 第三步：（按需）调整字幕裁剪坐标

原坐标针对《这就是中国》底部硬字幕条：
```python
# params.py
SUBTITLE_AREA = (235, 900, 235 + 1200, 900 + 90)  # (left, top, right, bottom)
```
《罗布奥特曼》字幕位置可能不同。**实测方法**：
1. 用播放器对 1080p 视频截一张带字幕的图，记下字幕条的像素坐标 `(左, 上, 右, 下)`；
2. 修改 `params.py` 的 `SUBTITLE_AREA`；
3. 重新跑第 5 步，抽检 `subtitle/*.json` 的 `text` 是否正确。

> 若字幕位置与默认一致，可跳过本步。

---

## 5. 第四步：离线构建字幕 JSON

```bash
cd VV_Rob
python main.py
```
- 遍历 `Videos/`，每秒抽帧 + 与爱染诚特征算余弦相似度（相似度**编码进帧文件名**，作为跨阶段数据总线）；
- PaddleOCR 识别 `SUBTITLE_AREA` 区域字幕；
- 输出 `subtitle/[P001]罗布奥特曼_01.json` 等（条目：`timestamp / similarity / text`）；
- 已生成字幕的视频会被跳过（支持断点续跑）。

### ⚠️ 文件名关键约束（务必遵守）
字幕 JSON 文件名**必须带 `[P编号]` 前缀**，例如 `[P001]罗布奥特曼_01.json`。
原因：
- 前端 `extractFrame` / `handleCardClick` 依赖 `[P###]` 计算拼图分组索引与 B 站跳转；
- `compress_subtitle.py` 与 Rust 检索均按 `[P###]` 解析集数。
`[P编号]` 可自定义（不一定要连续），但前缀格式不能改。

---

## 6. 第五步：生成本地检索引擎

### 6.1 前端本地库（必做，零额外依赖）
```bash
cd VV_Rob
python DataProcess/compress_subtitle.py
# 生成 subtitle_db.gz
cp subtitle_db.gz Web/subtitle_db
```
- 前端 `db_search.js` 已改为从 `./subtitle_db` 加载（原硬编码 vvdb.cicada000.work）。
- 浏览器首次访问会解压并缓存进 IndexedDB，之后离线可用。

### 6.2 本地 Rust 检索 API（可选）
```bash
cd VV_Rob/api/search && cargo build --release && cp target/release/subtitle_search_api ../api/
cd VV_Rob/api && pip install flask duckduckgo_search
python index.py   # 监听 http://localhost:8000
```
- 前端 `script.js` 的 `CONFIG.apiBaseUrl` 已设为 `http://localhost:8000`。
- 语义检索（RAG / bge-m3 / Qwen）属云端能力，本地版默认关闭；如需启用见第 8 步。

### 6.3 本地拼图预览（表情包图片来源）
参考 `DataProcess/merge_screenshot.py` + 二分索引脚本，把 `output_frames` 拼接为
`{group}.webp` 并生成 `.index`，放到前端可访问的静态目录。
前端 `CONFIG.imageBaseUrl` 已设为 `http://localhost:8088`，请在该端口伺服拼图与索引。

---

## 7. 第六步：启动前端并验证

```bash
cd VV_Rob/Web
python -m http.server 8080
# 浏览器打开 http://localhost:8080
```
验证清单：
- [ ] 标题显示"这就是罗布奥特曼·爱染诚"；
- [ ] 搜索关键词能返回爱染诚相关台词；
- [ ] 结果卡片显示预览图（需第 6.3 步拼图就绪）；
- [ ] 点击卡片在新标签页跳转 B 站对应时间点（需第 8 步填好 mapping）。

---

## 8. 第七步：填写集数→B 站映射

编辑以下两处，把占位 `epREPLACE01` 换成真实 B 站 `ep` 编号（BBDown 可解析得到）：
- `Web/mapping.json`
- `search/local/mapping.py` 的 `VIDEO_MAPPING`

key 必须与第 5 步生成的字幕 JSON 文件名**完全一致**（含 `[P编号]` 前缀）。

---

## 9. 可选：本地语义检索（bge-m3 + FAISS）

```bash
cd VV_Rob/search/local
python search.py   # 需要 torch / transformers / sentence-transformers / faiss-cpu
```
- 该链路模型体积大，建议在独立环境 / GPU 机器运行；
- 仅影响"AI 联网搜索"语义模式，不影响普通台词检索。

---

## 10. 已做的改动清单（相对原 VV）

| 文件 | 改动 |
|------|------|
| `params.py` | 注释改为《罗布奥特曼》/爱染诚；新增 `SUBTITLE_AREA`、`REQUIRED_RESOLUTION` 可配置项 |
| `Web/index.html` | 标题/Logo 文案改为"这就是罗布奥特曼·爱染诚"；移除原项目自有域名预连接 |
| `Web/script.js` | `CONFIG` 端点改为 `localhost` 占位；`randomStrings` 改为爱染诚相关文案 |
| `Web/db_search.js` | 数据库加载地址由 `vvdb.cicada000.work/subtitle_db` 改为本地 `./subtitle_db` |
| `Web/mapping.json` | 280 集《这就是中国》映射 → 《罗布奥特曼》模板（含 `[P###]` 约束说明） |
| `search/local/mapping.py` | 同上，保留 `get_video_url()` 辅助函数 |
| `CutSubtitle_paddleocr.py` / `CutSubtitle.py` | 字幕区域与分辨率改为读取 `params.py` 配置 |
| `tools/extract_faces.py` | **新增**：爱染诚人脸候选抽取脚本（检测+裁剪，供人工挑选） |
| `face_features_insightface.npz` | **运行时替换**：由张维为 → 爱染诚（第 3 步生成） |
| `Videos/`、`target/`、`faces_candidates/`、`Web/subtitle_db` | 运行期空目录（已建） |

**完全复用、未改动**：`api/`（Flask+Rust 调度）、`search/cloud/`（Cloudflare 向量/RAG）、
`Web/db_search.js` 的 LCS 算法、`DataProcess/`（部分）、`vercel.json`、`FaceRec*.py`、
`generate_features*.py`、`main.py` 主体逻辑。

---

## 11. 关键技术风险

1. **字幕位置差异**：奥特曼字幕条样式可能不同于原节目，务必实测 `SUBTITLE_AREA`。
2. **人脸特征质量**：`target/` 需足够多爱染诚清晰正脸，否则误检/漏检。
3. **相似度阈值**：不同光照、变身/皮套造型需调参，默认 0.5 可能偏高或偏低。
4. **OCR 误差**：特效字、艺术字识别易错，需人工抽检 `subtitle/*.json`。
5. **分辨率硬约束**：非 1920×1080 会被跳过，下载务必选 1080p。
6. **依赖体积**：torch/transformers 很大，语义检索链路建议独立环境。

---

## 12. 最小跑通路径（推荐顺序）

```
1. 下 1 集 1080p → Videos/
2. python tools/extract_faces.py → 挑图 → target/
3. python generate_features_insightface.py
4. （实测字幕坐标，必要时改 SUBTITLE_AREA）
5. python main.py  → 得到 subtitle/[P001]罗布奥特曼_01.json
6. python DataProcess/compress_subtitle.py → cp subtitle_db.gz Web/subtitle_db
7. cd Web && python -m http.server 8080 → 打开验证
```
完成第 7 步即得到一个**可本地搜索爱染诚台词**的最小可用系统。
表情包图片预览（拼图 webp）与 B 站跳转需补充第 6.3 / 第 8 步。
