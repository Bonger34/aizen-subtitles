# 《罗布奥特曼》·爱染诚 复刻运行手册

> 本仓库由 [Cicada000/VV](https://github.com/Cicada000/VV) 复刻而来，复用其全部代码与架构，
> 仅做最小改动：把视频源换成《罗布奥特曼》、识别人物换成**爱染诚**。
> 目标形态：**本地可运行版**（不部署 Vercel / Cloudflare，语义检索与 RAG 为可选）。

---

## 0. 整体原理（一句话）

```
视频(罗布奥特曼) ──main.py──► 每秒抽帧 + 爱染诚人脸相似度(编码进文件名) + PaddleOCR字幕
        │
        ▼
subtitle/[P###]标题.json   （条目: timestamp / similarity / text）
        │
        ├─ compress_subtitle.py ─► subtitle_db.gz  ──► 前端本地库(IndexedDB)
        │
        └─ merge_screenshot + 二分索引 + HTTP Range ─► 单帧 webp 拼图预览
        │
        ▼
Web 前端（搜索 / 复制 / 下载表情包）
```

---

## 1. 环境准备

### 1.1 Python 依赖（离线构建管线 + 本地 Flask API）
```bash
cd VV_Rob
pip install -r requirements.txt
# 额外需要：
pip install insightface onnxruntime paddleocr opencv-python pillow numpy tqdm
# 若用本地语义检索(FAISS)：
pip install torch transformers sentence-transformers faiss-cpu
```

### 1.2 Rust（可选，本地 /search 高速检索）
```bash
cd VV_Rob/api/search
cargo build --release
# 产物复制到 api/ 目录，命名为 subtitle_search_api
cp target/release/subtitle_search_api ../api/subtitle_search_api
```

### 1.3 前端静态服务（任意一种）
- Python：`python -m http.server 8080`（在 `VV_Rob/Web` 目录启动）
- 或 Node：`npx serve Web`

---

## 2. 第一步：获取视频（BBDown，B 站）

> 合规提示：仅限个人学习用途，注意版权。

1. 安装 [BBDown](https://github.com/nilaoda/BBDown)，登录 B 站账号。
2. 找到《罗布奥特曼》番剧，下载 **1080p（1920×1080）** 剧集。
   - ⚠️ **分辨率硬约束**：`CutSubtitle*.py` 内 `REQUIRED_RESOLUTION = (1920, 1080)`，
     非 1080p 的帧会被直接跳过、字幕为空。务必选 1080p。
3. 把视频文件放入 `VV_Rob/Videos/`，文件名建议带集数，例如：
   ```
   Videos/罗布奥特曼_01.mp4
   Videos/罗布奥特曼_02.mp4
   ```
   （`main.py` 会以"文件名去扩展名"作为字幕 JSON 的标题前缀。）

### 探针建议（最低成本、最高信息量）
先下 **1 集** 跑通，确认两件事再批量：
- 分辨率是否 1920×1080；
- 字幕条的实际像素坐标（见第 4 步），决定是否改 `params.py` 的 `SUBTITLE_AREA`。

---

## 3. 第二步：构建爱染诚人脸特征（核心）

原 `face_features_insightface.npz` 是**张维为**的特征，必须替换。

### 3.1 抽帧生成人脸候选
```bash
cd VV_Rob
python tools/extract_faces.py --interval 5 --max-faces 1
```
- 从 `Videos/` 每 5 秒抽 1 帧，检测并裁剪人脸到 `faces_candidates/`。
- 该脚本**只检测不比对**，所以 `target/` 为空也能跑。

### 3.2 人工挑选
打开 `faces_candidates/`，挑出**爱染诚的清晰正脸**（避开皮套/侧脸/遮挡），
复制到 `target/` 目录：
```bash
cp faces_candidates/罗布奥特曼_01_f000300_r0.jpg target/
# ……挑选 20~50 张质量稳定的正脸
```

### 3.3 生成特征向量（覆盖原文件）
```bash
python generate_features_insightface.py
# 生成 face_features_insightface.npz（爱染诚）
```

> 调参：`generate_features_insightface.py` 的 `det_thresh=0.5`；若漏检可下调。

---

## 4. 第三步：（按需）调整字幕裁剪坐标

原坐标针对《这就是中国》底部硬字幕条：
```python
# params.py
SUBTITLE_AREA = (235, 900, 235 + 1200, 900 + 90)  # (left, top, right, bottom)
```
《罗布奥特曼》字幕位置可能不同。**实测方法**：
1. 用播放器对 1080p 视频截一张带字幕的图，记下字幕条的像素坐标 `(左, 上, 右, 下)`；
2. 修改 `params.py` 的 `SUBTITLE_AREA`；
3. 重新跑第 5 步，抽检 `subtitle/*.json` 的 `text` 是否正确。

> 若字幕位置与默认一致，可跳过本步。

---

## 5. 第四步：离线构建字幕 JSON

```bash
cd VV_Rob
python main.py
```
- 遍历 `Videos/`，每秒抽帧 + 与爱染诚特征算余弦相似度（相似度**编码进帧文件名**，作为跨阶段数据总线）；
- PaddleOCR 识别 `SUBTITLE_AREA` 区域字幕；
- 输出 `subtitle/[P001]罗布奥特曼_01.json` 等（条目：`timestamp / similarity / text`）；
- 已生成字幕的视频会被跳过（支持断点续跑）。

### ⚠️ 文件名关键约束（务必遵守）
字幕 JSON 文件名**必须带 `[P编号]` 前缀**，例如 `[P001]罗布奥特曼_01.json`。
原因：
- 前端 `extractFrame` / `handleCardClick` 依赖 `[P###]` 计算拼图分组索引与 B 站跳转；
- `compress_subtitle.py` 与 Rust 检索均按 `[P###]` 解析集数。
`[P编号]` 可自定义（不一定要连续），但前缀格式不能改。

---

## 6. 第五步：生成本地检索引擎

### 6.1 前端本地库（必做，零额外依赖）
```bash
cd VV_Rob
python DataProcess/compress_subtitle.py
# 生成 subtitle_db.gz
cp subtitle_db.gz Web/subtitle_db
```
- 前端 `db_search.js` 已改为从 `./subtitle_db` 加载（原硬编码 vvdb.cicada000.work）。
- 浏览器首次访问会解压并缓存进 IndexedDB，之后离线可用。

### 6.2 本地 Rust 检索 API（可选）
```bash
cd VV_Rob/api/search && cargo build --release && cp target/release/subtitle_search_api ../api/
cd VV_Rob/api && pip install flask duckduckgo_search
python index.py   # 监听 http://localhost:8000
```
- 前端 `script.js` 的 `CONFIG.apiBaseUrl` 已设为 `http://localhost:8000`。
- 语义检索（RAG / bge-m3 / Qwen）属云端能力，本地版默认关闭；如需启用见第 8 步。

### 6.3 本地拼图预览（表情包图片来源）
参考 `DataProcess/merge_screenshot.py` + 二分索引脚本，把 `output_frames` 拼接为
`{group}.webp` 并生成 `.index`，放到前端可访问的静态目录。
前端 `CONFIG.imageBaseUrl` 已设为 `http://localhost:8088`，请在该端口伺服拼图与索引。

---

## 7. 第六步：启动前端并验证

```bash
cd VV_Rob/Web
python -m http.server 8080
# 浏览器打开 http://localhost:8080
```
验证清单：
- [ ] 标题显示"这就是罗布奥特曼·爱染诚"；
- [ ] 搜索关键词能返回爱染诚相关台词；
- [ ] 结果卡片显示预览图（需第 6.3 步拼图就绪）；
- [ ] 点击卡片在新标签页跳转 B 站对应时间点（需第 8 步填好 mapping）。

---

## 8. 第七步：填写集数→B 站映射

编辑以下两处，把占位 `epREPLACE01` 换成真实 B 站 `ep` 编号（BBDown 可解析得到）：
- `Web/mapping.json`
- `search/local/mapping.py` 的 `VIDEO_MAPPING`

key 必须与第 5 步生成的字幕 JSON 文件名**完全一致**（含 `[P编号]` 前缀）。

---

## 9. 可选：本地语义检索（bge-m3 + FAISS）

```bash
cd VV_Rob/search/local
python search.py   # 需要 torch / transformers / sentence-transformers / faiss-cpu
```
- 该链路模型体积大，建议在独立环境 / GPU 机器运行；
- 仅影响"AI 联网搜索"语义模式，不影响普通台词检索。

---

## 10. 已做的改动清单（相对原 VV）

| 文件 | 改动 |
|------|------|
| `params.py` | 注释改为《罗布奥特曼》/爱染诚；新增 `SUBTITLE_AREA`、`REQUIRED_RESOLUTION` 可配置项 |
| `Web/index.html` | 标题/Logo 文案改为"这就是罗布奥特曼·爱染诚"；移除原项目自有域名预连接 |
| `Web/script.js` | `CONFIG` 端点改为 `localhost` 占位；`randomStrings` 改为爱染诚相关文案 |
| `Web/db_search.js` | 数据库加载地址由 `vvdb.cicada000.work/subtitle_db` 改为本地 `./subtitle_db` |
| `Web/mapping.json` | 280 集《这就是中国》映射 → 《罗布奥特曼》模板（含 `[P###]` 约束说明） |
| `search/local/mapping.py` | 同上，保留 `get_video_url()` 辅助函数 |
| `CutSubtitle_paddleocr.py` / `CutSubtitle.py` | 字幕区域与分辨率改为读取 `params.py` 配置 |
| `tools/extract_faces.py` | **新增**：爱染诚人脸候选抽取脚本（检测+裁剪，供人工挑选） |
| `face_features_insightface.npz` | **运行时替换**：由张维为 → 爱染诚（第 3 步生成） |
| `Videos/`、`target/`、`faces_candidates/`、`Web/subtitle_db` | 运行期空目录（已建） |

**完全复用、未改动**：`api/`（Flask+Rust 调度）、`search/cloud/`（Cloudflare 向量/RAG）、
`Web/db_search.js` 的 LCS 算法、`DataProcess/`（部分）、`vercel.json`、`FaceRec*.py`、
`generate_features*.py`、`main.py` 主体逻辑。

---

## 11. 关键技术风险

1. **字幕位置差异**：奥特曼字幕条样式可能不同于原节目，务必实测 `SUBTITLE_AREA`。
2. **人脸特征质量**：`target/` 需足够多爱染诚清晰正脸，否则误检/漏检。
3. **相似度阈值**：不同光照、变身/皮套造型需调参，默认 0.5 可能偏高或偏低。
4. **OCR 误差**：特效字、艺术字识别易错，需人工抽检 `subtitle/*.json`。
5. **分辨率硬约束**：非 1920×1080 会被跳过，下载务必选 1080p。
6. **依赖体积**：torch/transformers 很大，语义检索链路建议独立环境。

---

## 12. 最小跑通路径（推荐顺序）

```
1. 下 1 集 1080p → Videos/
2. python tools/extract_faces.py → 挑图 → target/
3. python generate_features_insightface.py
4. （实测字幕坐标，必要时改 SUBTITLE_AREA）
5. python main.py  → 得到 subtitle/[P001]罗布奥特曼_01.json
6. python DataProcess/compress_subtitle.py → cp subtitle_db.gz Web/subtitle_db
7. cd Web && python -m http.server 8080 → 打开验证
```
完成第 7 步即得到一个**可本地搜索爱染诚台词**的最小可用系统。
表情包图片预览（拼图 webp）与 B 站跳转需补充第 6.3 / 第 8 步。

---

## 13. 文本质量复核流程（第 2 轮新增）

字幕库跑通后，**文本与画面是否一致**需要单独复核。以下是本轮实测有效的流程与坑。

### 13.1 先明确三个事实（都经过实测，不要再假设）

| 事实 | 说明 |
|---|---|
| 帧图与库文本是**一致**的 | 抽帧脚本用 OCR 校验过；不要怀疑帧图错配 |
| 库时间戳与视频时间轴**不同步** | 实测漂移 −3.2s ~ +1.1s 且随区间变化；**按时间戳取帧会大量读到邻句** |
| `cap.set(CAP_PROP_POS_MSEC)` **不可靠** | 复现率仅 37%、单次 615ms；找帧要靠内容匹配 |

### 13.2 主流程：全片字幕时间线重扫

```
每 0.25s 取一帧 (cap.grab 全片仅 78s/集)
  -> 字幕带纯白签名 (先按 >235 取掩膜再缩放二值化!)
  -> 仅当签名变化且距上次识别 >=0.5s 才 OCR(rec-only, 单次 46ms 与图尺寸无关)
  -> 与库条目做"同源相似度"对齐
```

**判定口径**：读到别句时相似度必然 <0.5，**同源的高相似差异才可信**——
不依赖时间戳，这是整套方法的基石。

**成本**：6.6 分钟/集（OCR 约 855 次/集）。脚本：`q_timeline.py` → `q_align_tl.py`。

### 13.3 三个已踩过的坑

1. **先缩放后阈值**：细笔画会被 `INTER_AREA` 平均掉 —— 实测某段字幕签名白像素归零，
   5 句一次 OCR 都没触发（事件数 72 → 修正后 606）。
2. **帧图白阈值过低**：960×540 上"明亮天空/彩虹背景"在 200 阈值下整片算白，行剖面把字幕行
   一起吞进大段再被密度过滤丢弃，导致大片 `seg=0`。帧图要用 **230 阈值 + 0.60 密度上限**。
3. **`del` 类方向不可靠**：新文本更短时多为漏读而非删除（`就你了罗布水晶` → `了罗布水晶`），
   只自动应用**纯插入型**（旧文本字符全保留 + 新文本多出字符）。

### 13.4 收尾：逐条人工核对 + 六项校验

所有修正**逐条对照帧图人工核对**后落盘（`q_sheet*.py` 生成对照表），0 条自动写入；
每次改动后跑：

```
make_subtitle_db.py → verify_consistency.py → duration_check.py
→ dup_check.py → stat_coverage.py → node verify_search.js → q_audit.py
```

### 13.5 走不通的路线（避免重复投入）

| 路线 | 结论 |
|---|---|
| 帧图放大 2 倍重试 | 566 条仅 2 条被证实 —— 放大只是插值，不产生新信息 |
| PP-OCRv6 SERVER 识别模型 | rapidocr 的 PPOCRV6 只有 small/medium，`resolve_model_key` 直接抛错 |
| 用帧图反查视频真实帧 | 64×36 全帧指纹会把"画面相似但字幕不同"的帧认成命中 |
| 窗口补扫（0.167s）替代帧图验证 | 107 分钟只证实 22%，性价比远低于帧图验证（3 分钟覆盖全部） |

---

## 14. 文本质量复核流程（第 4 轮新增：白边掩膜 + 帧图错位）

### 14.1 核心判据：字幕带用「白边掩膜」，不是白掩膜

中文字幕是**纯白字 + 黑描边**；白衬衫、白墙、亮天空则是一整块白、没有黑边。因此：

```python
白边掩膜 = 白像素(min(BGR) > 200) ∩ 邻域(9x9)存在暗像素(max(BGR) < 90)
```

实测在 `P01 t=537.81s`（字幕「甚至还有点」，画面是白衬衫）上：

| 掩膜 | y840~1075 剖面 | 能否切出字幕行 |
|---|---|---|
| 白掩膜 | 全程 130~275/1920 近乎平坦 | **不能**（阈值 0.12~0.9 都切成 235px 一大段，OCR 读空） |
| 白边掩膜 | y905~975 之外**恒为 0**，峰值 245@y964 | **能** |

再用连通域剔除"大块白"（衣物/背景，`MAX_CC_W=220`），只留单字量级的小块 → 实心字形。
实现见 `q_subband.py`（`band_rows` / `glyph_crop` / `read_subs`）。

### 14.2 三个会伪装成"读不出"的坑

| 坑 | 表现 | 修法 |
|---|---|---|
| `best` 初值取 0.0 且用严格 `>` | 旧文本只是少读几个字时 sim 恰为 **0.0**，真实读数被整个丢掉 | 初值改 −9.0 |
| 对整幅 1080p 做 dilate | 单帧 250ms | 只对扫描区做 → 单帧 165ms |
| 逐个连通域 `keep[lab==i]` | O(连通域数×全图)，演职员表画面单帧几十秒 | 一次 `np.isin(lab, good)` |

### 14.3 ⚠️ 最重要的一条：**帧图本身可能是错的，不能拿它改文本**

全库 7206 条的帧图来源实测：

| 来源 | 条数 | 占比 |
|---|---|---|
| 本秒帧 | 6098 | 84.6% |
| +1s 回退 | 826 | 11.5% |
| +2s 回退 | 219 | 3.0% |
| −1s 回退 | 57 | 0.8% |
| −2s / ±3s | 6 | 0.1% |

两处成因：

1. `make_frames_full.py` 用 `cap.set(CAP_PROP_POS_MSEC)` 取帧，**实际落点与请求时刻有偏差**
   （该 API 早期实测复现率仅 37%）。实例：`P16_9m27s.jpg` 画面上是「为什么要找我」，
   而库里 9m27s 是「你陪我一起吧」、9m28s 才是「为什么要找我」。
2. `make_frames_map.py` 有 `(0, 1, -1, 2, -2, 3, -3)` 的**回退窗口**，精确帧不存在时取邻秒帧。

后果：拿帧图去视频里反查、读出邻句，会**把对的文本改错**。第 4 轮 20 条候选里 **11 条是这么来的**——
最典型是奥特曼"轮唱"段，相邻两句恰好互为「泰罗奥特曼」/「银河奥特曼」，帧错位 1~2s 就"看起来像文本错"。

**按库时间戳直接读原片也不能当唯一依据**：库时间戳相对视频时间轴的漂移是**非单调、逐区间变化**的
（同一条实测里 P01 10m01s 对应视频 +1s，P17 9m31s 对应 −2.5s）。用 ±0.8s 窗口读全库，一致率只有 59%。

**可信度排序**：全片时间线（顺序取帧，无 seek）的顺序对齐 **>** 同一张图两条独立读数一致 **>** 单次读数。

### 14.4 修复错位帧：`q_reframe.py`

在 `t±1.6s` 内逐帧搜索**字幕读数与库文本一致（sim≥0.95）**的帧，按原规格重抽
（960×540、JPEG q80、等比缩放居中），命中即替换。用法：

```bash
python q_reframe.py --src q_reframe_tg.json --out Web/frames_fix --step 2
```

字幕持续 ≥0.5s，`--step 2`（0.083s）不会漏句。找不到匹配帧的条目原样保留并报告——
**这份"找不到匹配帧"的清单本身就是文本可疑清单**。

### 14.5 六项校验（不变）

```
make_subtitle_db.py → verify_consistency.py → duration_check.py
→ dup_check.py → stat_coverage.py → node verify_search.js → q_audit.py
```

### 14.6 文本质量部分的收尾状态（第 6 轮结束后定稿）

```
库 7206 → 6783(删 423: 带外 132 + 片尾/预告 283 + 字幕带判定 8)
修正 195(落盘 193 · 回退 2 · 异常 0), 验证覆盖 100%, 六项校验全通过
```

- **判定源文件**(各轮修正的可复现依据, 已入库, 不要删): `review/q_manual_verdicts.json`、
  `q_apply_result.json`、`q_apply2_result.json`、`q_revert_result.json`、`q_review9_apply.json`、
  `q_sheet10_apply.json`、`q_sheet11_apply.json`、`q_locate2_verdicts.json`、`q_suspect_class.json`、
  `q_targets.json`、`q_reframe_tg.json`。
- 其余中间产物(候选帧图 / 对照表 jpg / 时间线中间 json, 10507 个文件 / 1150 MB)已移出仓库,
  打包为上一级的 `_VV_Rob_review_archive_2026-08-21.zip`; 报告里出现的老路径按需解压回 `review/`。
- 全过程记录: `review/文本质量提升报告.md`(含当前最终状态与目录)。

### 14.7 ⚠️ `Web/assets/` 与 `Web/frames/` 的生命周期不同, 别混放

| 目录 | 性质 | 能否被清理 |
|---|---|---|
| `Web/frames/` | **可再生成的库配图**(6775 张 960×540)。凡"删除未被库引用的帧"的清理都可以动它 | 可以 |
| `Web/frames_removed/` | 已删除条目留下的帧(留档, gitignore) | 不还原 |
| `Web/assets/` | **入库的站点素材**(如搜索台背景 `hero_aisome.jpg`) | **绝对不要动** |

第 6 轮踩过一次: 搜索台背景原放在 `frames/P10_10m59s_hd.jpg`, 因为**只被 `style.css` 引用、
不被库引用**, 被"清理孤儿帧"顺手移进了 `frames_removed/` —— CSS 背景静默失效(浏览器不报错,
只是这一层不画), 首屏变成纯深色。两个教训:

1. 任何"未被库引用的帧"清理,**必须先排除被 `style.css` / `index.html` 引用的文件名**;
2. 只被样式引用的素材要放 `assets/`(该目录入库, 换台机器 clone 后也在), 不要放 `frames/`
   (`frames/` 整个是 gitignore 的, clone 后压根不存在)。

排查手法(30 秒): `Select-String -Path Web\*.css,Web\*.html,Web\*.js -Pattern "frames/[A-Za-z0-9_]+\.jpg"`
拿到所有非库引用, 再逐个 `Test-Path`。注意**用 `-LiteralPath`**, 库文件名里的 `[P01]` 会被通配符吃掉。

## 15. 浏览器实测站点(第 6 轮新增)


沙箱下 `agent-browser` 有两个坑, 各有一个可用的绕法:

1. **socket 目录不可写** —— 默认在 `~/.agent-browser`, 沙箱拒绝 → 指定到工作区内:

```powershell
$env:AGENT_BROWSER_SOCKET_DIR='<工作区>\.agent-browser'
```

2. **Chrome 起不来**(`Check failed: platform_channel.cc:108 拒绝访问` = mojo/crashpad 建命名管道被拦)
   —— 需要在放宽沙箱的条件下运行; 且**会话不能跨工具调用存活**, 所以"打开 + 交互 + 截图"必须在
   同一次调用里完成。可靠的写法是用分离进程打开页面, 再逐个查询:

```powershell
$env:AGENT_BROWSER_SOCKET_DIR="${W}\.agent-browser"
$env:AGENT_BROWSER_DEFAULT_TIMEOUT='20000'
cmd /c "start /b agent-browser open http://127.0.0.1:8099/ > `"$LOG`" 2>&1"   # 分离, 否则 open 不返回
for($i=0;$i -lt 30;$i++){ Start-Sleep -Milliseconds 1200; if(Select-String $LOG -Pattern '127.0.0.1:8099' -Quiet){break} }
agent-browser fill '#query' '水晶'; agent-browser click 'button.search-button'
agent-browser eval "var im=document.querySelector('.result-card img'); im.naturalWidth+'x'+im.naturalHeight"
agent-browser screenshot '<工作区>\VV_Rob\review\live_result.png'
```

站点本身要先起好: `python -m http.server 8099 --bind 127.0.0.1`(在 `VV_Rob/Web/` 下)。

### 15.1 ⚠️ 后台进程活不过工具调用 —— 这是最容易误判的一条

`run_in_background` 起的服务器、以及 `cmd /c start /b` 起的浏览器进程链, **在下一次工具调用时
可能已经不存在了**。症状有两种, 都很像"页面坏了"其实是环境没了:

* 浏览器报 `ERR_CONNECTION_REFUSED` / 页面变 `about:blank` —— 服务端或浏览器进程已被清理;
* 同样的命令上一次能跑通、这一次不行 —— 因为上一次是"冷启动成功"而这一次落进了竞态。

**可靠做法: 把服务器和浏览器放进同一次调用**, 用 `Start-Job` 起服务器、跑完就停:**

```powershell
$srv = Start-Job { Set-Location '<仓库>\VV_Rob\Web'
                   & '<python>' -m http.server 8099 --bind 127.0.0.1 }
Start-Sleep -Seconds 4
Invoke-WebRequest 'http://127.0.0.1:8099/index.html' -UseBasicParsing | Out-Null   # 等真正就绪
& 'C:\Program Files\Google\Chrome\Application\chrome.exe' --headless --disable-gpu --no-sandbox `
  --user-data-dir="$W\.cpA" --virtual-time-budget=15000 --window-size=1280,800 `
  --screenshot="$W\VV_Rob\review\shot.png" 'http://127.0.0.1:8099/index.html'
Stop-Job $srv; Remove-Job $srv -Force
```

两个附带的坑:

* **别用"处理 N 个请求后自动退出"的临时服务器** —— 配额被前一次 `--dump-dom` 用光后, 后一次
  截图会拍到 Chrome 的错误页; 而那张错误页截图有 20+ KB, **光看文件体积会以为成功了, 必须真的看图**;
* `--dump-dom`(取结构) 与 `--screenshot`(取渲染) 是两次独立启动, 各用一个 `--user-data-dir`,
  不要共用同一个;
* **`$env:TEMP` 在每次工具调用里可能是不同的目录** —— 别把 `git worktree`、临时服务器根目录这类
  需要跨调用访问的东西放在 `$env:TEMP` 下, 否则下一次调用就找不到了。用工作区内的固定路径,
  用完 `git worktree remove --force` + `git worktree prune` 清掉。

### 15.2 回退: 只用 git, 不要另造备份

**这一节是结论, 不是建议** —— 2026-08-21 折腾过一轮"标签 + 快照"的备份机制, 最后全删了:

| 造过的东西 | 为什么最后不要 |
|---|---|
| git 标签(`before-frontend-redesign` 等 4 个) | 其中两个指向同一个提交, 属重复; 且"某个中间状态"并不是独立回退目标 —— 退到它等于把后面的改动一并撤销 |
| 上一级目录的 `_backup_web_*` / `_site_snapshot_current` | 内容与工作区逐字节一致, **回退不到任何"过去"**; 价值只是"离线看一眼", 而 `git show <sha>:Web/index.html` 就能取回任何版本 |
| `_make_snapshot.py` 生成的单文件快照 | 同上。脚本留在仓库备用, 但不再维护快照产物 |

**git 已经够用**, 一条路走到底:

```powershell
git log --oneline                       # 找目标提交
git show <sha>:Web/style.css            # 取单个历史版本的文件
git checkout <sha> -- Web/              # 把 Web/ 退回那一刻
git worktree add <固定路径> <sha>        # 检出到另一目录做并排对比
```

渲染旧版看效果: `git worktree add` + `mklink /J` 把 `frames`/`assets` 接进去 + 起服务器。
(验证过可行: `review/rollback_old_design.png` 就是这么渲染出来的。)
**worktree 路径别放 `$env:TEMP`** —— 见 15.1 最后一条。

### 15.3 站点实测结果

**第 6 轮实测结果**(2026-08-21, 浏览器 = 本机 Chrome):

| 检查 | 结果 |
|---|---|
| 首屏 | 「爱染诚 台词档案馆」+「全 25 集 · 归档 6783 条 · 爱染诚登场 539 条」 |
| 页面/控制台错误 | 0(仅 PowerShell 读日志的句柄提示, 与页面无关) |
| 检索"水晶" | 20 条命中, 第 1 条 `[P05] 那儿应该有风属性的水晶 13m10s 匹配度 100.0%` |
| 结果帧图 | `frames/P05_13m10s.jpg` 自然尺寸 **960×540**, `complete=true`(真实加载成功) |
| 结果卡片 | 集数 + 台词 + 时间戳 + 匹配度 + 帧图 + 爱染诚角标, 与页面结构一致 |

