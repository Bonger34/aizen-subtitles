# 漏句检测方法论(连续读帧版)

> 本文记录 2026-09 对《罗布奥特曼》爱染诚字幕库做「漏句补全」的完整方法、参数、
> 验证结论与已知极限。所有结论都有实测数据支撑,可复现。

---

## 1. 问题背景

上游 [Cicada000/VV](https://github.com/Cicada000/VV) 用 **1fps 抽帧 + OCR** 建库,
每集 24 分钟只能取到约 2800 帧,而中文字幕每句仅显示 0.5–3 秒:

- 1fps 采样间隔 1 秒 > 大量短句的显示时长 → **成句漏检**
- 原始库每集约 199 条,而实际字幕句数约 330–400 条 → 覆盖率仅 ~55%

因此需要一套独立于 1fps 的「全量字幕句检测」流程。

---

## 2. 方法演进(失败路径同样重要)

| 版本 | 做法 | 结果 |
|---|---|---|
| v1 | 0.2s 逐帧 OCR,用 `cap.set(CAP_PROP_POS_MSEC)` 定位待验证帧 | ❌ **失败**:set+read 帧错位,验证帧与检测帧不一致 |
| v2 | 白像素区间检测 + set 定位 OCR | ❌ 失败:同上,所有「漏句」无法可靠验证 |
| **v3(现行)** | **全程 `cap.read()` 顺序解码,零 seek** | ✅ 成功 |

**关键教训**:`cv2.VideoCapture.set(POS_MSEC)` 在连续循环中**不可靠**,定位误差可达数秒。
任何「先检测、再回跳取帧」的流程都会引入错位。必须一次性顺序解码,或记录帧号后
**重新从头顺序读到该帧**。

---

## 3. 现行流程(`scan_cont.py`)

### 3.1 三遍扫描

```
第 1 遍: 顺序读全部帧 → 字幕带白像素占比序列 → 状态机切出字幕区间
第 2 遍: 顺序读全部帧 → 在采样帧位置保存字幕带 crop 到磁盘
第 3 遍: 对落盘的 crop 做二值化 + RapidOCR → 文本 → 与库比对
```

- **字幕带**:`SUBTITLE_AREA = (100, 895, 1820, 985)`(1920×1080)
- **白像素**:`gray > 245` 的像素占比
- **状态机**:`ON_TH=0.02` 判定出现、`OFF_TH=0.01` 判定消失(滞后防抖)
- **采样帧落盘**:OCR 与人工复核使用**同一份像素**,彻底消除「帧与文本不一致」

### 3.2 采样策略与数学保证

固定间隔 `S` 秒采样时,若某句字幕显示时长 `T ≥ S`,则**必有采样点落入其显示区间**
(固定网格 + 区间长度 ≥ 间隔)。命中率近似 `min(1, T/S)`。

| 采样间隔 S | 覆盖的短句时长 | 每集采样帧数 | 每集耗时 |
|---|---|---|---|
| 0.7s(基线) | T ≥ 0.7s | ~1500 | ~17 分钟 |
| 0.5s | T ≥ 0.5s | ~2000 | ~25 分钟 |
| **0.35s(现行)** | **T ≥ 0.35s** | ~2600 | ~35-40 分钟 |

实测漏检的短句(「晚安」「可恶」「跳踢」「明白了」「朝阳」「后来」「男人嘛」)显示时长约
0.4–0.6 秒,0.7s 采样命中率仅 57–86%,故必须降到 0.35s。

### 3.3 库匹配

- 窗口 ±5s(分析阶段用 ±8s),字符重合度 < 0.6 判为「未覆盖」
- 未覆盖 → 人工/自动复核 → 回填

---

## 4. GPU 环境(踩坑记录)

### 4.1 onnxruntime-gpu 版本与 CUDA 链不匹配

`onnxruntime-gpu 1.29` 的 `onnxruntime_providers_cuda.dll` 依赖 **CUDA 13** 的
`cublas64_13.dll`,而驱动 UMD 为 CUDA 13.3,加载失败后**静默回退 CPU**(日志仅一行
`No Registered plugin EP device found for 'CUDAExecutionProvider'`),导致 5–15s/帧。

**解决**:降级到 `onnxruntime-gpu 1.24.4`(CUDA 12 链),手动下载 cu12 DLL 到
workspace 目录,启动时 `os.add_dll_directory()` + PATH 注入(沙箱禁止写 site-packages):

```
_nv_dlls/     cudart64_12 / cublas64_12 / cudnn64_9(9.15 cu12) / cufft64_11 / curand64_10 / nvrtc64_120_0
_ort124/      onnxruntime-gpu 1.24.4(解压 wheel, sys.path 前置)
```

### 4.2 慢帧根因:动态输入 shape

RapidOCR 的 rec 每批取「批内最大宽度」、批大小随检测框数变化 → 每个新 shape 触发
CUDA 首次执行,单帧耗时 6–30 秒且不可预测(占 15–30% 帧)。

**解决**:固定输入形状

```python
'Rec.rec_img_shape': [3, 48, 1536],   # 固定宽度(32:1 宽比,兼容单行 30 字)
'Rec.rec_batch_num': 1,               # 固定 batch,消除 shape 抖动
```

修复后稳定 **0.6–0.9s/帧**,全量 25 集从「预估 25 小时」降到 7 小时。

### 4.3 已排除的加速路线

| 尝试 | 结果 |
|---|---|
| crop 缩放到 0.7x / 0.5x | ❌ 不加速(瓶颈是 det 的固定输入 resize + 每帧调用开销),0.5x 反而更慢 |
| det 换 MOBILE / SMALL / TINY | ❌ PP-OCRv6 mobile 不支持 multi-lang;其他型号模型下载被沙箱拒绝;SMALL 并不更快 |
| 白像素变化驱动采样(状态驱动) | ❌ **失败**:相似长度句子切换时白像素总量变化 <5%,短句全未触发打点 |

---

## 5. 已知极限与剩余风险

| 风险 | 说明 | 现状 |
|---|---|---|
| 显示时长 < 0.35s 的闪句 | 采样间隔的数学下界 | 未发现实例 |
| 单字台词(「喂」「嗯」) | `pass_filter` 要求 ≥2 汉字 | **确定排除**,如需可放宽 |
| 片尾名单区台词 | ED 段每秒都有名单帧,OCR 与名单混叠 | 已逐条人工判定 |
| 白像素占比 < 1% 的字幕 | 彩色/描边/极细字 | ON_TH 已降至 0.01 检验 |
| OCR 识别失败 | 生僻字/遮挡 | 多帧采样缓解,人工抽查修正 |

---

## 6. 复现步骤

```powershell
# 0. 环境(一次性)
$env:PATH = '<workspace>\_nv_dlls;' + $env:PATH     # cu12 DLL
# _ort124 由 scan_cont.py 自动 sys.path 前置

# 1. 全量扫描(每集 ~35 分钟, 25 集约 15 小时)
$env:SCAN_ON_TH='0.01'; $env:SCAN_OFF_TH='0.005'
$env:SCAN_STEP='0.35';  $env:SCAN_TAG='dense_'
python scan_cont.py P01 P02 ... P25
# 产物: review/dense_cont_Pxx.json (seqs: 每采样帧文本; cands: 库未覆盖候选)

# 2. 覆盖率分析
python cont_gap.py          # 全量缺口统计 -> review/cont_gap.json
python gap_classify.py      # 分类: drop噪声 / 歌词名单 / 真候选

# 3. 回填(帧提取 + 库 + frames_map)
python cont_fill_all.py P03 ... P25
python fill_conflicts.py    # 秒冲突补录(顺延空闲秒)
python fill_final.py        # 收尾剩余真候选

# 4. 数据一致性校验
python dup_check.py         # 同秒重复
python verify_consistency.py # 库 ↔ frames_map ↔ 帧文件 三方一致
python verify_search.js     # node 端模拟搜索, 断言帧文件存在

# 5. 重建前端库
python make_subtitle_db.py
python stat_coverage.py
```

---

## 7. 短句补扫实战结论(0.35s 全量重扫)

2026-09 用 `SCAN_STEP=0.35` 对全部 25 集重扫(耗时 ~19 小时,区间数从 ~410 增至 ~440,
采样帧数从 ~1500 增至 ~2600),与基线三方对比:

| 指标 | 数值 |
|---|---|
| 原始新发现候选 | 446 条 |
| 剔除 ED 名单段(22m10s+) | −251 条 |
| 正片段候选 | 186 条 |
| **人工筛选后确认的真漏句** | **86 条** |

补回的漏句**全部是 2-4 字短句**,例如:
`晚安 / 可恶 / 跳踢 / 明白了 / 朝阳 / 总之 / 危险 / 好痛 / 好咧 / 走了 / 吵死了 / 闭嘴 /
快去 / 达令 / 冲啊 / 滚开 / 少啰嗦 / 多亏了你 / 应该能赶上了 / 站住 / 其实 / 扔掉了`

**结论**:基线 0.7s 采样对短句的命中率约 57-86%,0.35s 采样能覆盖所有 ≥0.35s 的字幕;
本次补扫后,「检测到的字幕区间」内已无未处理候选。

---

## 8. 下探验证:0.35s 采样是否已饱和?

为进一步确认 0.35s 采样的完整性,对 P01/P13 用 **0.17s 采样**(间隔减半)做了下探扫描:

| 集 | 采样间隔 | 采样帧 | 唯一句 | 0.35s 漏掉且库未覆盖 |
|---|---|---|---|---|
| P01 | 0.35s | 2642 | 434 | — |
| P01 | **0.17s** | 4744 | 523 | **5 条(逐条核查:全为 OCR 残片/ED 名单,无真台词)** |
| P13 | 0.35s | 2792 | 422 | — |
| P13 | **0.17s** | 5023 | 461 | **4 条(同样是残片:一厦/州不人一/静有/出妙)** |

**交叉验证**:0.35s 找到的全部短句(晚安/可恶/跳踢/明白了/朝阳/后来/男人嘛)在 0.17s 中**全部存在**;
两轮扫描的唯一句差异集中在片尾名单与长句的 OCR 切分变体。

**结论**:0.35s 采样在实际内容上已饱和 —— 再加密一倍采样,真实漏句为 **0**。
理论上剩余的漏句只可能是显示时长 <0.35s 的极短闪句(实测未发现实例)。

---

## 9. 数据现状(2026-09-10)

| 指标 | 数值 |
|---|---|
| 集数 | 25 |
| 字幕条数 | **6999** |
| 帧图 | 9000+ 张(960×540, quality 90) |
| 帧覆盖率 | 100%(0 缺失) |
| 一致性 | 库条目 = frames_map 键 = 帧文件,0 重复 / 0 孤儿 |
| 每集唯一字幕句 | 约 330-440(0.35s 采样口径) |
| 短句补扫 | 已执行(0.35s 全量重扫,补回 86 条) |

