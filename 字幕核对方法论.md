# 《罗布奥特曼》爱染诚 · 字幕核对方法论

> 25 集 6783 条台词是怎么一条条核出来的 —— 判据、参数、踩过的坑、走不通的路。
>
> **两份文档的分工**：`复刻运行手册.md` 讲**怎么做**（可照抄的命令）；本文讲**判定与配套** ——
> 凭什么改、凭什么删、**漏句怎么找**、帧图与时间戳怎么处理、哪些路试过但走不通。
> 各专项的实测数据与推导过程见 `review/复核总报告.md`。
>
> 本文提炼自 285 个一次性脚本（原 `scripts/oneoff/`）里写下的注释与判据。那些脚本已从仓库
> 删除；需要看原始代码时用 `git show <提交>:scripts/oneoff/<脚本名>.py` 取回。

---

## 1. 判定口径

这一节是全文的核心。字幕文本的对错判定，全部建立在下面这几条上。

### 1.1 只用「同源」的读数

读到别句时，相似度必然 <0.5；**同源的高相似差异才可信**。这条不依赖时间戳，是整套方法的
基石（全片时间线重扫用的就是它，见手册第 11 节）。

### 1.2 相似度的初值必须低于任何可能值

旧文本只是少读几个字时（如 `赢了` vs `我们赢了`），相似度**恰好为 0.0**。若初值取 0.0 并用
严格的 `>` 比较，这条真实读数会被整个丢掉 —— 表现为「定位到了却读不出」。

> 同理这条也解释了为什么 `q_locate2` 把初值写成 −9.0。

### 1.3 不要「两处分别取最大值」

要**先**定位「与旧文本最相似的那一段读数」（即字幕所在那一段），**再**在同一段读数上比新文本。
若两处分别取最大值，会拿画面里另一处无关文字去「支持」新文本。**实测踩过。**

### 1.4 多源互证优先于单源

可信度排序：

```
全片时间线（顺序取帧、无 seek）的顺序对齐
  > 同一张图两条独立读数一致
    > 单次读数
```

「帧图读数 = 时间线新文本 ≠ 旧文本」是两个**独立来源**互证，可以落盘；单次读数只能进人工清单。

### 1.5 只用「文本自身」作判据

**基于「该时刻读数」的判据会大量误伤。** 实测两例：

| 误伤 | 原因 |
|---|---|
| P18 1m22s「制作T恤吗」 | 被关键词「制作」命中（画面里另有「制作」字样） |
| P05 5m10s「要勒紧这里对吗」 | 被邻句的假名带偏 |

所以删除带外条目的脚本只认文本自身的特征（假名 / 繁体 / 版权卡字样），不认「这一秒读到了什么」。

### 1.6 方向可靠才自动应用

新文本比旧文本短时，多为**漏读**而非删除（`就你了罗布水晶` → `了罗布水晶`）。所以只自动应用
**纯插入型**：旧文本字符全保留、新文本多出字符。其余一律进人工清单。

### 1.7 部分证实：可以「半信」

帧图读数是旧文本的**超集或子序列**时，可以部分证实：

- 帧图读数 ⊇ 旧文本 → 旧文本没有凭空捏造；
- 旧文本是帧图读数的**子序列**且帧读更长 → 「补全型」候选，方向可靠。

### 1.8 顺序可靠，时间戳不可靠

库时间戳有 ±1s 级错位，**但库条目与字幕事件的先后顺序是可靠的**。定位一条条目的真实文本时，
用「上一条/下一条库条目各自认领了哪个字幕事件」做**顺序夹逼**，真身必然落在两者之间。

### 1.9 比对前先做简繁归一

库里与识别结果混用繁体（別 / 奧 / 強 / 裡）。不归一会把同一句判成两句 —— 既造成假「漏句」，
也让去重失效。

### 1.10 保护：VL 的「短句改长句」大概率是误读

VL（PaddleOCR-VL）会把**画面里的其他文字**当成字幕读出来。两条保护：

- **低相似度替换 + 短句 → 拒绝**（实测 `好了` 被读成 `凉州府`）；
- VL 会把画面文字**拼进文本头尾**（`…郊游Schallplatt`、`CH₄谁会…`）→ 切掉头尾的 ASCII 字母段
  （≥2 字母），剩余若是有效中文则用切后文本。

### 1.11 帧身份 + 纯插入，比「该秒读数」更可靠

判断「帧错了」还是「文本错了」时，**帧身份（字幕带 IoU ≥0.7）+ 纯插入关系**优先于「该秒读到什么」
—— 后者被库时间戳漂移污染（全库本就有 17% 条目在该秒读到邻句）。实测据此改回
`P01 20m41s[赢了]`、`P02 0m26s[接来]` 两条此前被误判为「帧错、文本对」的条目。

---

## 2. 字幕定位与裁剪

### 2.1 白边掩膜

中文字幕是「纯白字 + 黑描边」，白衬衫/白墙/亮天空是一整块白、没有黑边。所以判据是
**白像素 ∩ 邻域存在暗像素**。完整的实测数据与实现见手册第 12.1 节。

### 2.2 字幕不总是在同一条带里 —— 三区域检测

除了已知字幕带，还要单独扫「**仅 LOW 区间**」：现有字幕带完全没覆盖、只有**带下方**有白字的
区间。带外字幕（版权卡、预告卡顶部字幕）就落在那里。

| 区域 | 坐标(1920×1080) |
|---|---|
| BAND(现有) | x 100–1820, y 895–985 |
| LOW(新增) | x 100–1820, y 985–1062 |
| TOP(仅统计, 未补录) | x 100–1820, y 62–200 |

实测「仅 LOW」合计 848.6 秒、「仅 TOP」643.0 秒；其中只有 **6 条**是真台词（双语字幕块，见 5.9）。

### 2.3 ⚠️ 裁剪区高度会制造「碎片条目」

**库里那些莫名其妙的超短文本，很多不是 OCR 读错，而是字幕行伸出了裁剪区下缘被切掉。**

- 判据：主文字行带**伸出**字幕带下缘（`low_text_frames`）；
- 手法：把裁剪区加高到 `y 850~1075` 重读一遍（`reocr_short`）；
- 「读得更好」的定义：新文本更长 **且** 旧文本基本被新文本包含（同一句，只是补全了）。

文字行的切分：**间隔 >6px 视为不同文字行**。

实测裁切比例（抽样 1500 张已存帧）：文字行带下边界低于 985 的占 **6.1%**；完全落在带内的 73.0%。
注意已存帧是「带内有白像素」才被抽出来的，**完全落在带外的字幕根本不会进入统计** ——
6.1% 只是下界。

### 2.4 几何能区分字幕与演职员表

字幕**居中且不横跨全宽**；演职员表**横跨两侧**。纯几何侦察（不做 OCR）就能把两者分开
（`q_geom` / `q_scan_ep`）。

### 2.5 字幕带坐标

1080p 原帧：`params.py` 里 `SUBTITLE_AREA = (100, 895, 1820, 985)`，即字幕带 `y 895~985`；
复核脚本的扫描区再往下探到 `y 1075`。
960×540 帧按比例：`895/1080×540 ≈ 447`，`985/1080×540 ≈ 492`。

实测普通对白文字行落在 y 910–968（完整在带内），资料卡字幕落在 y 964–1022（下缘被裁）。

---

## 3. 漏句检测

上游用 **1fps 抽帧 + OCR** 建库，这对短句有结构性漏检 —— 这是本项目的第一性问题，
也是最终补入 203 条、库从 6999 涨到 7206 的原因。各轮实测数据见 `review/复核总报告.md` 第 1 节。

### 3.1 问题的量级

每集 24 分钟只能取到约 2800 帧，而中文字幕每句仅显示 0.5–3 秒：

- 1fps 采样间隔 1 秒 > 大量短句的显示时长 → **成句漏检**；
- 原始库每集约 199 条，而实际字幕句数约 330–400 条 → **覆盖率仅 ~55%**。

漏掉的句子**几乎都夹在两条已收录条目之间**。根因：当年的补扫只把**新检测到的区间**追加进库，
而**已覆盖区间内新出现的句子没有被追加** —— 连续对白里白像素检测一直处于 ON（没有回落到 OFF
的机会），整段变成一个区间，后续补扫就把它当作「已覆盖」跳过了。

### 3.2 方法演进：两条失败路径（同样重要）

| 版本 | 做法 | 结果 |
|---|---|---|
| v1 | 0.2s 逐帧 OCR，用 `cap.set(CAP_PROP_POS_MSEC)` 定位待验证帧 | ❌ **失败**：set+read 帧错位，验证帧与检测帧不一致 |
| v2 | 白像素区间检测 + set 定位 OCR | ❌ 失败：同上，所有「漏句」无法可靠验证 |
| **v3（现行）** | **全程 `cap.read()` 顺序解码，零 seek** | ✅ 成功 |

**关键教训**：`cv2.VideoCapture.set(POS_MSEC)` 在连续循环中**不可靠**，定位误差可达数秒。
任何「先检测、再回跳取帧」的流程都会引入错位。必须一次性顺序解码，或记录帧号后
**重新从头顺序读到该帧**。

### 3.3 现行流程：三遍顺序扫描

```
第 1 遍: 顺序读全部帧 → 字幕带白像素占比序列 → 状态机切出字幕区间
第 2 遍: 顺序读全部帧 → 在采样帧位置保存字幕带 crop 到磁盘
第 3 遍: 对落盘的 crop 做二值化 + RapidOCR → 文本 → 与库比对
```

- **白像素**：`gray > 245` 的像素占比；
- **状态机**：`ON_TH=0.02` 判定出现、`OFF_TH=0.01` 判定消失（滞后防抖）；实测可降到 0.01/0.005；
- **采样帧落盘**：OCR 与人工复核使用**同一份像素**，彻底消除「帧与文本不一致」。

### 3.4 采样密度的数学保证

固定间隔 `S` 秒采样时，若某句字幕显示时长 `T ≥ S`，则**必有采样点落入其显示区间**
（固定网格 + 区间长度 ≥ 间隔）。命中率近似 `min(1, T/S)`。

| 采样间隔 S | 覆盖的短句时长 | 每集采样帧数 | 每集耗时 |
|---|---|---|---|
| 0.7s（基线） | T ≥ 0.7s | ~1500 | ~17 分钟 |
| 0.5s | T ≥ 0.5s | ~2000 | ~25 分钟 |
| **0.35s（现行）** | **T ≥ 0.35s** | ~2600 | ~35–40 分钟 |
| 0.17s（下探验证用） | T ≥ 0.17s | ~4700–5000 | 加倍 |

实测漏检的短句（「晚安」「可恶」「跳踢」「明白了」「朝阳」「后来」「男人嘛」）显示时长约
0.4–0.6 秒，0.7s 采样命中率仅 57–86%，故必须降到 0.35s。

> ⚠️ **「0.35s 已饱和」这个结论被推翻了。** 项目中期只在 P01/P13 两集做过 0.17s 下探就宣布
> 「真实漏句为 0」，**不能推广到全系列** —— 随后在 P22/P24 的 480 秒样本里就查出 3 条真漏句，
> 且都是**当年完全读得出来**的短句（带内白占比 0.028/0.031/0.054，高于阈值 0.02），
> 属**建库环节的丢失**而非「读不出」。据此估计漏收率量级约 1%~3%。四轮全量补扫随后展开。

### 3.5 库匹配与补录

- 窗口 ±5s（分析阶段用 ±8s），字符重合度 < 0.6 判为「未覆盖」；
- 未覆盖 → 逐条到视频复核（候选秒 ±0.4s 三帧 OCR，候选文本需在窗口内 **≥80% 复现**）→ 回填；
- **第 4 轮修正的过滤口径**：不再按内容词（奥特曼/怪兽/水晶/形态…）排除「图鉴解说」——
  那会误杀真台词（`奥特战士真的超棒的`、`有人要订购10000件`、`你哪来的自信啊` 等 43 条）。
  改为**按跨集重复 / 固定时间段**识别歌词与职员表。

### 3.6 已知极限

| 风险 | 说明 | 现状 |
|---|---|---|
| 显示时长 < 0.35s 的闪句 | 采样间隔的数学下界 | 四轮均未发现实例 |
| 单字台词（「喂」「嗯」） | `pass_filter` 要求 ≥2 汉字 | **确定排除**，如需可放宽 |
| 片尾名单区台词 | ED 段每秒都有名单帧，OCR 与名单混叠 | 已逐条人工判定 |
| 白像素占比 < 1% 的字幕 | 彩色/描边/极细字 | ON_TH 已降至 0.01 检验 |
| **完全落在字幕带之外的中文字幕** | 见 2.2 / 5.9 | 经查全是版权卡/歌词/图鉴/预告，非台词 |
| OCR 太差的真句 | 残留在「噪声」桶里的极少数样本 | 读出的文本无法辨识，无法入库 |

---

## 4. 帧图

### 4.1 帧图会错位，不能拿它改文本

`make_frames_map.py` 有 `(0, 1, -1, 2, -2, 3, -3)` 的**回退窗口**；`make_frames_full.py` 用
`cap.set(CAP_PROP_POS_MSEC)` 取帧，**实际落点与请求时刻有偏差**（该 API 早期实测复现率仅 37%）。

**当前 6783 条的实测偏移分布**（`docs/frames_map.js` 键与帧文件名逐条比对，见总报告 2.1）：

| 帧图来源 | 条数 | 占比 |
|---|---:|---:|
| 本秒帧 | 5688 | 83.9% |
| +1s 回退 | 823 | 12.1% |
| +2s 回退 | 213 | 3.1% |
| −1s 回退 | 53 | 0.8% |
| ±3s | 2 | 0.0% |
| **非本秒帧合计** | **1095** | **16.1%** |

> ⚠️ **这个数字量的是文件名偏移，不是内容错。** 第 4/5 轮的逐条重抽把 1060 + 3879 张按内容
> 换成了「字幕读数与库文本一致（sim≥0.95）」的帧，写回**同名文件** —— 所以名字差 1~3 秒
> ≠ 画面是别的句子。**拿帧图去视频里反查、读出邻句，会把手里的正确文本改错。**

### 4.2 按库时间戳直接读原片也不能当唯一依据

库时间戳相对视频时间轴的漂移是**非单调、逐区间变化**的（同一条实测里 P01 10m01s 对应视频
+1s，P17 9m31s 对应 −2.5s）。所以不能用一个全局偏移去校准（见第 6 节）。

### 4.3 选帧策略

| 场景 | 取法 |
|---|---|
| 有字幕持续窗口 | 本句显示窗口 `[sec, next_sec-1]`，上限 `sec + WINDOW_MAX`；遍历整个窗口，取相似度**最高**的一帧（保证是持续期间最清晰的一张） |
| 窗口内无主演 | 取相似度最高者（时间零漂移，不丢帧） |
| 兜底 | `ts → ±1 → ±2`，**首次匹配即存**（覆盖率优先） |
| 密集补齐 | 窗口 `[sec−0.5, sec+3]`；上一句留给下一条处理，不越 `ts−0.5` 太多 |

抽帧时**同时保存该脸的 512 维 embedding** 供后续复用 —— 对已裁切的小脸图重新检测，召回率极低。

**错位帧重抽**（`q_reframe.py`）：在 `t±1.6s` 内逐帧搜索**字幕读数与库文本一致（sim≥0.95）**的帧，
按原规格重抽（960×540、JPEG q80、等比缩放居中），**写回同名文件**，命中即替换。
找不到匹配帧的条目原样保留并报告 —— **这份「找不到匹配帧」的清单本身就是文本可疑清单**。

### 4.4 共用配图：成因是「它自己那张帧不存在」

首轮 119 组条目共用同一张配图，修复后 **10 组**，**当前 8 组**（全部是相邻秒的同屏两行，无害）。

逐条查下来，成因不是映射写错，而是**出错那条自己名字的帧根本不存在**（106 组里 104 组的
`{集}_{时间戳}.jpg` 在磁盘上确实没有），于是回退窗口把它接到了邻条的帧上。
**修法是给出错条目补抽它自己那句话的画面，而不是改映射。**

### 4.5 孤儿帧：先分「纯重复」，再逐张定性

处理顺序（顺序不能颠倒，否则工作量虚高）：

1. **按内容哈希**分离「与已被引用的帧字节完全相同」的纯重复 → 直接删；
2. 剩下的才是「独有内容」，逐张 OCR 定性：
   - 先查库匹配（**字幕与职员表叠在同一帧**时也能识别出中文台词）；
   - 再退回「是否日文文本」（职员表特征）；
   - 最后做**整帧 OCR**，找**不在字幕带内**的中文字幕（如次回预告卡顶部字幕）。

### 4.6 兜底帧：宁缺毋滥

用「任意字幕文本兜底」补出来的帧，画面大概是相邻句 —— 这类帧要**主动撤销**。
注意连带效应：若该帧名同时被同秒冲突的另一条引用，**那条也要一并回空**。

---

## 5. 带外内容与去重

### 5.1 什么算带外

四类：**版权卡 / OP·ED 歌词 / 演职员表 / 次回预告·图鉴解说**。竖排文字、纯数字、乱码标题卡同理。

### 5.2 ⚠️ 段位判据优先于内容判据 —— 但段位只用于缩小范围，不作删除依据

**只在片头/片尾/版权卡这些固定段位上排除，不按内容词排除。** 按内容词排除会误伤真台词
（见 1.5 的两例）。

实测的真实段位（取代此前 5 篇文档里互相矛盾的 4 个「片尾段边界」）：

| 段 | 实测边界 | 内容 |
|---|---|---|
| 片尾歌词 | 22m20s – 23m45s | OP·ED 歌词，每集重复 |
| 图鉴 / 下集预告 / 人物介绍卡 | 23m45s – 24m15s | 解说词、身高体重数据句 |
| 版权声明卡 | 不定位（每集都有，共 25 处） | `中国大陆地区独家版权 上海新创华文化发展有限公司` |
| 片头 | ≤ 1m30s | 版权卡 / 标题卡 |
| **P25** | **片尾自 ~21m 开始** | 台词与演职员表**叠在同一帧**，必须单独收紧 |

> ⚠️ **踩过**：曾按「≥21m30s 属非台词区」删，而 P01 21m30s「你怎么会在这里」、
> 21m48s「今晚吃寿喜锅」、22m13s「还是先和妈妈说声生日快乐吧」**都是真台词** ——
> 按区间删会误删约 800 条对话。**段位只用来缩小人工范围，最终一律只看文本自身。**

### 5.3 跨集重复 = 歌词碎片

同一文本（或高相似）出现在 **≥3 集** → 判为片头/片尾歌词碎片，排除。这是最有效的歌词识别法：
歌词每集都出现，台词不会。

### 5.4 去重规则

| 范围 | 规则 |
|---|---|
| 批内 | 同集同句 `±6s` 只留一条 |
| 全局 | 同文本且 ts 差 `≤3s` 视为重复，**保留更早一条** |
| 相邻 | 相邻采样文本相同或互为子串时，保留首次 |
| 同 ts | 同时间戳且 LCS ≥ 0.6 → 保留最长 |

### 5.5 ⚠️ 两道保险：虚词 + 结构性

删除必须留后路，否则会删掉真台词。**实测踩过**：`P18 7m15s 凑潮50岁`、`P25 23m43s 路上小心`
都被区间规则误删 —— 因为它们不含虚词，没被保险拦住。

1. **虚词保险**：含台词虚词（的/了/吗/呢/吧/啊…）的一律保留，**宁可漏删**；
2. **结构性保险**：凡出现在历次「已应用修正」清单里的条目**不删** —— 它们已被人工确认是台词。

### 5.6 P25 的特例：台词与职员表叠在同一帧

P25 片尾的台词与演职员表**叠在同一画面**，库文本把两者混在了一起。**不能按区间删**，
只能逐条看上下文，只保留台词部分。

### 5.7 文本清洗的 5 条确定性规则

按顺序判定，任一命中即过滤：

| # | 规则 | 依据 |
|---|---|---|
| 1 | 含日文假名（≥1，含长音符 `ー`） | 歌词 / 日文原句；有效中文台词不会出现假名 |
| 2 | 无中文字符 | 纯 ASCII 乱码 / 英文 / 数字 |
| 3 | 中文占比 < 40% 且含大量非中文字符 | 水印、装饰残片 |
| 4 | 含繁体字 | 简中字幕不应含繁体；职员表多为繁体/日文汉字 |
| 5 | 年份 + 赛事/日期模式 | **VL 日期幻觉**残留，见下 |

两条细节：

- **只删字母，不删数字** —— 否则误伤 `15年之久`、`第12集`；
- **白名单**：有效台词里的合法英文只有 `PDF` / `GPS` / `NASA` 三个。
  `OK` / `TV` / `AI` 经全库扫描确认**只出现在乱码串里**，不保护。

### 5.8 VL 日期幻觉

PaddleOCR-VL 对 OP 歌词、模糊帧会输出**无假名、无 ASCII** 的幻觉文本，常规规则过滤不掉。
特征模式：`2023年夏季国际高山滑雪锦标赛`、`2023年1月1日星期一一` —— 用「年份 + 赛事/日期」识别。

### 5.9 双语字幕块：带外漏掉的真台词

P18 一类时刻是**双语字幕块**：日文在上、中文在下，字号更小、整体更靠下（中文行 y≈980–1040）。
实测字幕带 y895–985 内的 `>245` 白像素占比只有 **0.005 ~ 0.011**，低于触发阈值 **0.02**，
因此整段未被检测到（对比：普通单行台词为 **0.05~0.14**）。全系列因此**漏掉 6 条真台词**，
已全部核实补入库（6999 → 7002）。

---

## 6. 时间戳

### 6.1 漂移是非单调的

同一条实测里，P01 10m01s 的字幕出现在视频 602.0s（**+1s**），P17 9m31s 的字幕出现在视频 568.5s
（**−2.5s**）。所以**不能用一个全局偏移去校准**。

### 6.2 用 DP 做最优分配

把「条目 → 真实秒」当成分配问题，**最小化总位移**。代价取位移的**平方**，以抑制
「个别条目被推很远」的离群解。约束：**保持画面先后 + 同秒唯一**；
排序要按**内容真实秒**（当前顺序本身已被打乱，不能按原时间戳排）；帧文件同步重命名为
`{集}_{新时刻}.jpg`。

### 6.3 改动阈值

- 定位可用性用 MAD 阈值判定（脚本里取 20~30 不等）；峰值置信度 `0.75`；
- 「需要改动」= 与实测内容时间相差 **≥ OFF_MIN**（**测量噪声约 ±1s**，低于这个数不动）；
- 当前时间戳**已超出视频长度** → 必定错误，无条件改。

### 6.4 峰值不明确时的兜底

用**可靠邻居的偏移线性插值**补出内容时间，而不是丢弃。

### 6.5 ⚠️ 帧号必须夹到 ≥ 0

`0m00s` 那条往前取 0.8s 会得到**负帧号**，会让取帧的 `while` 永远匹配不上 ——
结果是**整集一路 grab 到底、所有条目都读成空**。实测 `P01`/`P02` 共 **276 + 283 条全空**。

### 6.6 成因：文档里有什么、代码里能读出什么

> ⚠️ **先说结论：这个项目从没把「库时间戳为什么错」写成成因。** 复核做完了，但成因始终是空白。
> 下面把散落的线索和代码里能读出来的机制归拢起来，并标明哪条被实测支持、哪条不成立 ——
> 供以后的人接手时不必从头再猜一遍。

**文档里出现过的四类说法，逐条定性：**

| 位置 | 说法 | 是不是时间戳错位的成因 |
|---|---|---|
| `review/复核总报告.md` 2.1（原 附三 D） | 「根因: 帧图错位有两个来源」—— `make_frames_full.py` 的 `cap.set(POS_MSEC)` 落点偏差 + `make_frames_map.py` 的 ±3s 回退窗口 | **不是**。这条链管的是 `docs/frames/` 的**配图**，与库里的 `timestamp` 字段无关。两件事被混成了一件 |
| 复核总报告 2.2（原 附四） | 「库时间戳普遍比真实显示时刻**晚约 1s**」 | 只给现象，**没给机制** |
| 复核总报告 1.x（原 附一） | 「时间戳与视频时间轴漂移 −3.2s ~ +1.1s 且随区间变化」 | 只给现象 |
| 复核总报告 3.4 / 6.3 | 「根因是时间戳漂移，读到的常是邻句」 | 把漂移**当成**解释，是循环论证 |

**代码里能读出来的两条机制（文档此前都没记）：**

**M1 —— 采样周期是 0.959s，不是 1.0s。** `main.py` 这条建库链**根本不 seek**：
`FaceRec_insightface.py:118` 顺序 `cap.read()`，`:151` 用 `time_sec = frame_count / frame_rate`
算秒（标号本身准，floor 到秒）。但 `:109` 是

```python
interval = max(1, int(frame_rate / fps))     # int(23.98 / 1) = 23, 不是 24
```

采样周期 = `23 / 23.98` = **0.959s**。两个后果：

- 采样点永远不与整秒对齐；
- 单句显示 0.6s 时，有约 **37%** 的概率整句落在两个采样点之间 —— 这是**漏句的一个独立来源**，
  与「1fps 太稀」不是同一件事；
- 时间戳记的是「抽到的那一帧所在的整秒」，而这一帧必然落在字幕的**显示期间**，
  所以它**只可能 ≥ 字幕真正出现的秒**。这正好解释了附四观测到的「+1s 偏置」——
  它是**有方向的**，不是随机抖动。

**M2 —— 补录时的「顺延空闲秒」。** 原 `漏句检测方法论.md` §6 的复现步骤里有一行

```
python fill_conflicts.py    # 秒冲突补录(顺延空闲秒)
```

即：补录一条真漏句时，若目标秒已被别的条目占用，就把候选**顺延到邻近的空闲秒**而不是丢弃。
后果是条目**落在它根本不属于的秒上，偏差可任意大**，而且**只影响个别条目、周围条目正常**。

> 这条线索一度在仓库文档里查不到 —— 它只出现在那份 §6 里，而 §6 整块引用的是已删的
> 一次性脚本，合并《漏句检测方法论》进本文时被一并去掉了。现在补回。
> 原文见 `git show 5fa4e02:漏句检测方法论.md`。

**实测形态支持 M2、不支持 M1。** 收尾时顺序读 P20 原片（23.98 fps，零 seek）实测
4m34s–4m52s，与库条目对齐：

| 库文本 | 库里 | 原片实测 | 偏差 |
|---|---|---|---|
| 行了行了 去那边吧 | 4m34s | 4m34.0 | 0 |
| 要是你带回来一个很轻浮的男人 | 4m35s | 4m35.5 | +0.5 |
| 注意后面 有客人 | 4m39s | 4m39.5 | +0.5 |
| 那 那个 欢迎光临 | 4m40s | 4m40.5 | +0.5 |
| **爸爸可接受不了** | 4m41s | 4m37.5 | **+3.5** |
| **爸爸会寝食难安的哦** | 4m43s | 4m38.5 | **+4.5** |
| **有客人来了** | 4m46s | 4m40.0 | **+6.0** |
| **这边是今日新款** | 4m45s | 4m42.5 | **+2.5** |
| **请这边走** | 4m44s | 4m45.0 | **−1.0** |
| 喂 勇海你要去哪里啊 | 4m47s | 4m47.5 | +0.5 |

即：**紧邻的正常条目都落在 ±0.5s，出问题的 5 条偏 2.5~6.0s** —— M1 是全局均匀的 ~1s 偏置，
产生不了 +6s；「顺延到空闲秒」恰好给出「个别条目跳到邻近秒、周围正常」的形态。

**仍未定案的部分。** 以上是单点证据（一段 18 秒、5 条），不足以判定全库的偏移构成。
要定案需要做一次**全库逐条偏移测量**：顺序读全片、在每条的时间戳上读一次原片，
统计偏移的分布形态 —— 是「全局 +1s」「个别跳秒」还是「区间性漂移」各占多少。
成本见 `review/复核总报告.md` 2.2 节（约 1.2 小时）。**这一步没做，所以成因目前只能说是
「M1 解释全局偏置、M2 解释个别大偏」，比例未知。**

---

## 7. 环境与性能

### 7.1 CUDA / onnxruntime

| 事实 | 处理 |
|---|---|
| CUDA/cuDNN DLL 不在 site-packages | 放在工作区 `_nv_dlls`，**必须在 `import onnxruntime` 之前** `os.add_dll_directory` 注册 |
| ORT 1.29 是 CUDA13 链，与驱动不兼容 | 从工作区 `_ort124` 前置 ORT **1.24.4**（CUDA12 链） |
| 沙箱限制 site-packages 写入 | 绕过 pip，手动下载 `nvidia-*-cu12` wheel 解压取 DLL |

**症状与根因**：`onnxruntime-gpu 1.29` 的 `onnxruntime_providers_cuda.dll` 依赖 **CUDA 13** 的
`cublas64_13.dll`，而驱动 UMD 为 CUDA 13.3，加载失败后**静默回退 CPU**（日志仅一行
`No Registered plugin EP device found for 'CUDAExecutionProvider'`），导致 5–15s/帧。

```
_nv_dlls/     cudart64_12 / cublas64_12 / cudnn64_9(9.15 cu12) / cufft64_11 / curand64_10 / nvrtc64_120_0
_ort124/      onnxruntime-gpu 1.24.4(解压 wheel, sys.path 前置)
```

### 7.2 慢帧的根因是「动态输入 shape」

RapidOCR 的 rec 每批取「批内最大宽度」、批大小随检测框数变化 → 每个新 shape 触发 CUDA 首次执行，
单帧耗时 6–30 秒且不可预测（占 15–30% 帧）。

**固定 rec 输入宽度 + `batch=1`** 可彻底消除 CUDA 首次执行慢帧：

```python
'Rec.rec_img_shape': [3, 48, 1536],   # 固定宽度(32:1 宽比, 兼容单行 30 字)
'Rec.rec_batch_num': 1,               # 固定 batch, 消除 shape 抖动
```

修复后稳定 **0.6–0.9s/帧**。按列投影裁到文字实际水平范围，能减少 rec 输入宽度 ——
这**直接决定耗时**。`rec` 单张的 **46ms 是固定启动开销**，与图尺寸无关（batch 无效），
因此成本只取决于**调用次数**。

### 7.3 ⚠️ 限流不能省

逐帧扫描时画面运动会 `diff` 频繁超阈值。**不加限流实测：91 秒窗口触发 716 次识别（7.9 次/秒）**。
加入「距上次识别至少 8 帧」的限流后降到 **129 次 / 173 秒**，识别次数降 5.5 倍，
而字幕持续通常 ≥0.5s，不会漏句。

### 7.4 已排除的加速路线

| 尝试 | 结果 |
|---|---|
| crop 缩放到 0.7x / 0.5x | ❌ 不加速（瓶颈是 det 的固定输入 resize + 每帧调用开销），0.5x 反而更慢 |
| det 换 MOBILE / SMALL / TINY | ❌ PP-OCRv6 mobile 不支持 multi-lang；其他型号模型下载被沙箱拒绝；SMALL 并不更快 |
| 白像素变化驱动采样（状态驱动） | ❌ **失败**：相似长度句子切换时白像素总量变化 <5%，短句全未触发打点 |
| 帧图放大 2 倍重试 | ❌ 566 条跑完仅 2 条被证实 —— 放大只是插值，不产生新信息 |
| PP-OCRv6 **SERVER** 识别模型 | ❌ rapidocr 的 PPOCRV6 只提供 small/medium，`resolve_model_key` 直接抛错 |

### 7.5 沙箱带来的三处改写

| 限制 | 改写 |
|---|---|
| `site-packages` 不可写 | DLL 解到工作区，运行时注册搜索目录 |
| `multiprocessing` 管道被禁 | 抽帧改**顺序**执行（282 帧约 2–4 分钟，可接受） |
| 回收站不可用时拒绝 `rmtree` | 不预先清空旧目录，改为覆盖写入（残留空目录无害） |

### 7.6 四个会静默搞乱结果的工具坑

- **PowerShell 管道把中文按 GBK 解码** → 脚本输出一律用 ASCII 键名与列宽对齐；
- **`glob` 会把 `[P01]` 当字符类** → 遍历库文件必须用 `listdir` + 字符串匹配；
- **PowerShell 变量名不区分大小写** → `$R` 与 `$r` 是同一个变量，用 `$r = <sha>` 会把仓库路径
  覆盖掉，产出假的「目录不存在」；
- **`git ls-files` 不加 `-c core.quotepath=false`** → 中文路径被转义成 `"\345\244..."`，
  `open()` 抛错，扫描脚本会**静默跳过**这些文件。

---

## 8. 复核流程自身的坑

复核脚本是「写一次、跑很多次」的，所以状态残留比逻辑错误更危险。

| 坑 | 后果 | 修法 |
|---|---|---|
| 已回退的修正仍留在判定清单里 | 每跑一次「应用」脚本就被**重新应用**一次。实测：P02 9m50s、P04 3m39s 被改回错文本 | 应用脚本显式跳过已回退项 |
| 预演模式写了结果文件 | 被写成 `reverted: []`，审计脚本把已回退的 2 条**重新当成异常** | 预演**绝不写盘** |
| 逐集结果的 `rows` 是累加的 | 每集文件都含前面各集的行，统计虚高 | 合并时按 `(ep, ts)` 去重 |
| 「挑出不以 `-` 开头的参数」当集号 | `--src` 的值被误认成集号，只处理「1 集」就退出 | 用统一的顺序解析循环 |
| 半集跑过的产物混进统计 | 「没跑到」被误判成「未找到匹配帧」 | 只统计**整集都跑过**的集 |
| 只以 report 文件为准 | 重抽脚本中途停止时不写 report，判据落空 | 以**产物**（新帧是否存在）为准 |
| 扫描脚本取帧记账 off-by-one | 每采样点累积 1 帧，一集内误差可达**上百秒**，候选位置全错 | `grab()` 计数要把 `retrieve()` 消费的那一帧算进去 |
| 对照表用整帧缩略图 | 1560×3750 被读图工具缩到 516×1240，字幕只剩约 5px，两次都白做 | **只裁字幕带并保持原尺寸**（960×540 的 y436~504），整张表 1580×780 不被缩放 |

---

## 9. 关键参数表

| 参数 | 值 | 出处（原脚本） |
|---|---|---|
| 视频帧率 | **23.98 fps**(全 25 集实测 23.976~23.980, 1920×1080) | 本次实读 `CAP_PROP_FPS` |
| 视频时长 | 24m14s ~ 24m42s(每集约 24.5 分钟) | 同上 |
| 字幕带白像素阈值 | `gray > 245` | `scan_cont` |
| 白像素状态机 | ON 0.02 / OFF 0.01（可降到 0.01 / 0.005） | `scan_cont` |
| 漏句密扫步长 | 0.25s / 0.35s（基线 0.7s） | `scan_series_dense` / `scan_cont` |
| 漏句库匹配窗口 | ±5s（分析 ±8s），重合度 <0.6 判未覆盖 | `cont_gap` |
| 漏句候选复核 | 候选秒 ±0.4s 三帧，需 ≥80% 复现 | `dense3_verify` |
| rec 输入宽度 | 1536（32:1），batch=1 | `scan_cont` |
| rec 单次固定开销 | 46ms（与图尺寸无关） | — |
| 白边掩膜 | 白 `min(BGR)>200` ∩ dilate(暗 `max(BGR)<90`, 9×9) | `q_subband` |
| 掩膜连通域上限 | `MAX_CC_W=220` / `MAX_CC_H=110` | `q_subband` |
| 放宽档（救偏暗/偏小字） | `w_thr=180, max_cc_w=400, min_cc_a=60` | `q_subband` |
| 字幕带（1080p） | y 895~985，扫描到 1075 | `band_clip` / `low_text_frames` |
| 三区域检测 | BAND y895–985 / LOW y985–1062 / TOP y62–200 | `scan_ext_ocr` |
| 加高裁剪区 | y 850~1075 | `reocr_short` |
| 文字行切分 | 间隔 > 6px 视为不同行 | `low_text_frames` |
| 帧图白阈值（960×540） | 230 + 密度上限 0.60（200 会把亮天空整片算白） | `q_verify` |
| 密集补齐窗口 | `[sec−0.5, sec+3]` | `extract_frames_dense` |
| 密扫帧窗 | 前 500 帧 / 后 3000~4000 帧 | `extract_frames_scan3/5/7` |
| 帧图相似度命中阈值 | ≥ 0.95 | `q_reframe` |
| 帧图重抽窗口 | t ± 1.6s | `q_reframe` |
| 帧身份可靠性 | 字幕带 IoU ≥ 0.7 | `q_locate2` |
| 共用配图补抽包含度阈值 | ≥ 0.8（同时充当安全阀） | `repair_missing` |
| 相似度初值 | −9.0（必须低于任何可能值） | `q_locate2` |
| 同 ts 去重 LCS | ≥ 0.6 | `scan_noise` |
| 全局去重 | 同文本且 ts 差 ≤3s | `calibrate_ts` |
| 批内去重 | 同集同句 ±6s | `dense3_curate2` |
| 跨集重复判歌词 | 同文本出现在 ≥3 集 | `dense3_curate` |
| 片尾歌词 / 图鉴段 | 22m20s–23m45s / 23m45s–24m15s；P25 自 ~21m | `scan_ext_ocr` |
| VL 可疑判定 | ASCII ≥2 字母 **或** 假名 ≥1 | `hybrid_recheck` |
| VL 帧窗口 | ts±1 三帧取最长 | `hybrid_recheck` |
| VL 日期幻觉模式 | 年份 + 赛事/日期 | `dense3_curate` |
| 人脸聚类阈值 | 余弦距离 t=0.35（相似度 0.65） | `cluster_faces` |
| 时间戳改动阈值 | ≥ OFF_MIN（噪声 ±1s） | `retime_from_locate` |
| 时间戳定位 MAD 阈值 | 20~30；峰值置信度 0.75 | `locate_all` |
| 逐帧扫描限流 | 距上次识别 ≥ 8 帧 | `q_fill` |
| 抽样随机种子 | 20260913 | `q_sample_tg` |

---

## 10. 这些结论原来在哪

285 个一次性脚本（原 `scripts/oneoff/`，966 KB / 24912 行）已从仓库删除。
它们是**只能读、不能跑**的研究留档 —— 其中 212 个写死了本机绝对路径，200 种被引用的中间产物
也已随 `review/` 中间产物一起移出仓库。

需要看某个判据的原始实现时：

```bash
git log --oneline -- scripts/oneoff/          # 找出删除前的提交
git show <提交>:scripts/oneoff/q_tsread.py    # 取回单个脚本
```

保留下来的是它们的**结论**（本文 + `review/复核总报告.md` + `review/` 里的 8 个判定源文件），
以及**可重跑的构建与校验链**（`scripts/pipeline/`，23 个 `.py` + `verify_search.js`）。
