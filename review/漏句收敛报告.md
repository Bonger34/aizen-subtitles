# 漏句收敛报告(第三轮迭代)

## 直接回答

**收敛了。** 用"扫描 → 与全库比对 → 逐条到视频复核 → 入库 → 再扫描比对"的办法迭代三轮,
每轮的残留量下降约一个数量级:

| 轮次 | 处理对象 | 结果 |
|---|---|---|
| 第 1 轮 | 983 条疑似漏句(全系列密扫 + 全库匹配) | 复核通过 750 → 策展后**入库 175 条** |
| 第 2 轮 | 扩库后重判,残留 417 条疑似漏句 | 复核通过 369 → 排除片尾/图鉴段 224、歌词碎片 124、片段 9 → **入库 13 条** |
| 第 3 轮 | 再重判,正片残留 63 条 | 其中真台词候选 9 条 → 复核通过 **入库 8 条**(1 条库里已有劣质变体) |

库条目:**7003 → 7183(+180 条,约 +2.6%)**。

## 现在的残留是什么

第 3 轮把"疑似漏句"逐条列出来后,正片段只剩 63 条,构成:

| 类别 | 例子 | 性质 |
|---|---|---|
| OCR 碎字 | `二二二二`、`坐1心`、`咽心`、`心正`、`聖亨`、`元工`、`心5科` | 不是字幕,是画面白字/局部字幕的误读 |
| 已有条目的片段 | `再快`(库中`再快一点`)、`可是`、`旋风`(库中`旋风光线`) | 已被收录,只是读短了 |
| 片头片尾歌词碎片 | `守全部`、`重合行`、`乗越戦`、`決口揺`、`信抜` | 每集都出现的日文歌词,按"只算台词"排除 |
| 版权卡 / 标题卡 | `中国大随地区独版棚上海速`、`集全世界都在等着我` | 非台词 |
| **真台词** | 本轮全部 9 条已复核并入库(如 `逗你的我开玩笑的`、`但我还是很想知道`、`我们得想办法对付它`) | — |

## 为什么现在可以说"收敛"

1. **位置已可信**:修掉了扫描脚本的取帧记账 bug(off-by-one,误差按采样点累积到上百秒),
   并逐条用画面 OCR 复核(候选文本需在 ±0.4s 内 ≥80% 复现)——第 2、3 轮的入库项全部通过复核;
2. **匹配已到全库**:候选不再只跟 ±5s 邻域比,而是与全库 7183 条做双向模糊匹配;
3. **残留量按数量级下降**:983 → 417 → 63(正片),且最后 63 条里真台词只有 9 条,已全部处理;
4. **反复独立发现同一批问题**:第 3 轮正片残留里没有再出现"全新句子"。

## 仍未覆盖的部分(明确说明)

1. **片尾段(≥22m30s,P25 为 ≥21m)**:该段是演职员表 / 片尾歌词 / 下集图鉴解说,按"只算台词"口径未补录;
2. **时长 <0.25s 的字幕**:密采间隔 0.25s,理论上可能漏,未观察到实例;
3. **被过滤掉的"不像中文台词"(198 条)与"噪声/纯数字"(936 条)** 里可能仍有个别真句
   (通常是 OCR 读得太差),这部分未逐条人工核对;
4. **少量条目的文本质量**:如 `整坐城币得救`(应为「整座城市得救了」)、`释放山志的招振油`(已修)等,
   属于文本不精确而非漏句。

## 校验

条目 **7183**;缺键 0 / 缺帧 0 / 孤儿键 0 / 越界 0 / 同秒重复 0;搜索 409 命中 0 缺帧;覆盖率 100%;
共用配图 8 组(全部为"同屏两行"无害组)。帧文件 7298 个 517.3 MB。

| 文件 | 内容 |
|---|---|
| `review/converge.json` | 扩库后的残留候选(含 dialogue / short 两类) |
| `review/dense4_report.txt` | 第 2 轮 369 条复核明细 |
| `review/dense2/*.json` | 修正版全系列扫描结果 |
